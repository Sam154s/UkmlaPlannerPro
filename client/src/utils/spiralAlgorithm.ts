export { generateSpiralTimetable } from '../algorithms/generate';

// Re-export types for backward compatibility
export type {
  SpiralConfig,
  StudyBlock,
  UserPerformance,
  UserEvent
} from '../types/spiral';